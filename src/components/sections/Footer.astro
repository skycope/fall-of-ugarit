---
const currentYear = new Date().getFullYear();

const links = [
  { label: 'Synopsis', href: '#synopsis' },
  { label: 'Background', href: '#background' },
  { label: 'Reviews', href: '#reviews' },
  { label: 'Author', href: '#author' },
  { label: 'Buy', href: '#buy' },
];
---

<footer class="bg-ink text-cream py-16">
  <div class="container mx-auto px-6 md:px-8 lg:px-12 max-w-7xl">
    <div class="grid md:grid-cols-3 gap-12 mb-12">
      <!-- Brand -->
      <div>
        <h3 class="font-serif text-2xl font-medium mb-4 italic">
          The Fall of Ugarit
        </h3>
        <p class="text-cream/60 text-sm leading-relaxed">
          A novella exploring myth, memory, and love across six thousand years.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h4 class="font-medium text-sm uppercase tracking-wider text-gold mb-4">
          Navigation
        </h4>
        <nav class="space-y-2">
          {links.map(link => (
            <a
              href={link.href}
              class="block text-cream/70 hover:text-cream transition-colors duration-200"
            >
              {link.label}
            </a>
          ))}
        </nav>
      </div>

      <!-- Contact -->
      <div>
        <h4 class="font-medium text-sm uppercase tracking-wider text-gold mb-4">
          Get in Touch
        </h4>
        <p class="text-cream/60 text-sm mb-4">
          For orders, press enquiries, or just to say hello.
        </p>
        <a
          href="mailto:contact@example.com"
          class="text-cream hover:text-gold transition-colors duration-200"
        >
          contact@example.com
        </a>
      </div>
    </div>

    <!-- Divider -->
    <div class="h-px bg-cream/10 mb-8"></div>

    <!-- Bottom -->
    <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-cream/40">
      <p>
        &copy; {currentYear} Michael Cope. All rights reserved.
      </p>
      <p>
        Made with care in South Africa
      </p>
    </div>
  </div>
</footer>
